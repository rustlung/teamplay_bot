# 🚀 Инструкция по выгрузке проекта в GitHub

## ✅ Что уже защищено

В проекте уже настроен `.gitignore`, который **исключает** из репозитория:
- `.env` - ваш файл с токеном бота (ЗАЩИЩЁН ✅)
- `venv/` - виртуальное окружение
- `__pycache__/` - кэш Python
- `*.db` - база данных с задачами
- `*.csv` - временные CSV файлы

## 📋 Пошаговая инструкция

### 1. Создайте файл `.env` локально (если ещё не создан)

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_реальный_токен_от_BotFather
```

**⚠️ ВАЖНО:** Этот файл НЕ попадёт в GitHub благодаря `.gitignore`!

### 2. Инициализируйте Git репозиторий

Откройте терминал в папке проекта и выполните:

```bash
git init
```

### 3. Проверьте, что .env не будет загружен

Выполните команду для проверки:

```bash
git status
```

Вы **НЕ** должны видеть файл `.env` в списке. Если видите - остановитесь и проверьте `.gitignore`!

### 4. Добавьте файлы в Git

```bash
# Добавляем все файлы (кроме тех, что в .gitignore)
git add .

# Проверяем, что добавилось
git status
```

**Убедитесь, что `.env` НЕ в списке добавленных файлов!**

### 5. Создайте первый коммит

```bash
git commit -m "Initial commit: Telegram bot for team task management"
```

### 6. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите кнопку "New repository" (зелёная кнопка справа вверху)
3. Укажите название, например: `teamplay-bot`
4. **НЕ** добавляйте README, .gitignore или LICENSE (у нас уже есть)
5. Нажмите "Create repository"

### 7. Свяжите локальный репозиторий с GitHub

GitHub покажет команды, выполните их:

```bash
# Укажите адрес вашего репозитория
git remote add origin https://github.com/ваш-username/teamplay-bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### 8. Проверьте результат

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что **НЕТ** файла `.env`
3. Убедитесь, что **ЕСТЬ** файл `.env_example`

## 🔐 Проверка безопасности

### ✅ ЧТО ДОЛЖНО быть в репозитории:
- `main.py`
- `config.py`
- `requirements.txt`
- `.gitignore`
- `.env_example` ← пример без реального токена
- `README.md`
- Все папки: `database/`, `handlers/`, `keyboards/`

### ❌ ЧТО НЕ ДОЛЖНО быть в репозитории:
- `.env` ← ваш файл с реальным токеном
- `venv/` ← виртуальное окружение
- `*.db` ← база данных
- `__pycache__/` ← кэш Python

## 🛡️ Что делать, если .env случайно попал в Git

### Если ещё НЕ отправили на GitHub (git push):

```bash
# Удалите файл из индекса Git (но не с диска)
git rm --cached .env

# Создайте новый коммит
git commit -m "Remove .env from tracking"
```

### Если УЖЕ отправили на GitHub:

**⚠️ КРИТИЧНО:** Токен скомпрометирован! Немедленно:

1. **Удалите старый токен бота:**
   - Откройте [@BotFather](https://t.me/botfather)
   - Отправьте `/revoke`
   - Выберите вашего бота
   - Получите новый токен

2. **Очистите историю Git:**

```bash
# Удалите файл из истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

# Принудительно отправьте изменения
git push origin --force --all
```

3. **Или проще:** Удалите репозиторий на GitHub и создайте новый

## 📝 Инструкция для других разработчиков

Добавьте в README секцию для тех, кто клонирует репозиторий:

```markdown
### Настройка после клонирования

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/username/teamplay-bot.git
   cd teamplay-bot
   ```

2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Создайте файл .env:
   ```bash
   # Скопируйте пример
   cp .env_example .env
   
   # Откройте .env и укажите свой токен
   # BOT_TOKEN=ваш_токен_от_BotFather
   ```

5. Запустите бота:
   ```bash
   python main.py
   ```
```

## 🔄 Регулярные обновления

После внесения изменений в код:

```bash
# Проверьте изменения
git status

# Добавьте изменённые файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

## 💡 Полезные команды

```bash
# Посмотреть список файлов в Git
git ls-files

# Проверить, что .env игнорируется
git check-ignore -v .env

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git diff
```

## 🌟 Рекомендации

1. **Никогда** не отключайте `.gitignore`
2. **Всегда** проверяйте `git status` перед коммитом
3. **Используйте** `.env_example` как шаблон для других разработчиков
4. **Документируйте** все переменные окружения в README
5. **Создавайте** отдельные токены для production и development

## 📞 Что делать при проблемах

Если вы не уверены, что `.env` защищён:

1. **НЕ ДЕЛАЙТЕ** `git push`
2. Проверьте: `git ls-files | grep .env`
3. Если .env есть в списке - используйте команды из раздела "Что делать, если .env попал в Git"
4. Обратитесь за помощью

---

**Помните:** Безопасность токена = безопасность вашего бота! 🔐

